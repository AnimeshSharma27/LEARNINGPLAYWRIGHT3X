---
description: Explain a programming concept and save it as a markdown note in "IQ Notes"
---

The user's question/topic is: $ARGUMENTS

Explain this concept clearly and save it as a `.md` file inside the `IQ Notes` folder
(create the folder if it doesn't exist), following this style:

1. Give a short intro to the concept.
2. If the user's currently open/selected file is relevant, use it as a concrete example
   (read the file first and quote the relevant snippet).
3. Include a **comparison/explanation table** (Markdown table) as the main teaching aid —
   mirror the structure used in `IQ Notes/Source_code_ByteCODE_Binary_IQ.md`:
   - one row per aspect/dimension being compared
   - a "how it flows / how it works" section below the table when applicable
   - a short "key takeaway" table or bullet list at the end
4. Keep language simple and beginner-friendly, but technically accurate.
5. Name the file descriptively based on the topic, ending in `_IQ.md`
   (e.g. `Closures_IQ.md`, `Var_Let_Const_IQ.md`), and save it to `IQ Notes/<name>_IQ.md`.
6. After saving, reply with a concise summary (table only, no restating the whole file)
   and confirm the saved file path.
